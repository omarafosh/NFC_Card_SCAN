# دليل تشغيل قارئ NFC على الماك (macOS)

هذا المجلد يحتوي على كل ما تحتاجه لتشغيل قارئ الـ NFC الخاص بنظام الخصومات على أجهزة الماك وتحويله إلى تطبيق مستقل (لا يحتاج إلى تثبيت يدوي للمكتبات لاحقاً).

## 🚀 خطوات التشغيل لأول مرة:

1. **إعداد الاتصال**:
   - افتح ملف `.env` (إذا لم يكن موجوداً، قم بإنشائه) باستخدام أي محرر نصوص.
   - تأكد من وضع رابط Supabase ومفتاح API الخاص بك.

2. **تشغيل سكريبت الإعداد ال آلي**:
   - افتح برنامج **Terminal**.
   - اسحب مجلد `dist-mac` وأفلته في الـ Terminal، أو استخدم الأمر `cd` للوصول إليه.
   - قم بتشغيل الأمر التالي:
     ```bash
     chmod +x mac-setup.sh && ./mac-setup.sh
     ```

3. **ماذا سيحدث؟**:
   - سيقوم النظام بتحميل حزمة `pkg` والتعريفات اللازمة.
   - سيتم إنشاء ملف جديد في المجلد اسمه **`nfc-reader-mac`**.

## 🍏 كيفية الاستخدام اليومي:
بعد إتمام الخطوات أعلاه، ليس عليك سوى الضغط مرتين على ملف **`nfc-reader-mac`** ليعمل القارئ فوراً. لن تحتاج لتثبيت Node.js أو أي مكتبات برمجية أخرى على أجهزة الماك الأخرى، فقط انقل هذا الملف واستخدمه.

## 📋 أوامر مفيدة:
- لمعرفة أرقام الأجهزة المتاحة في نظامك: `node list-terminals.js` (يتطلب Node.js).
- لتغيير إعدادات الجهاز الحالي: عدل ملف `terminal-config.json`.

---
**NFC Discount System - v0.2.0**
